hosts = [
    "legio-0",
    "legio-1",
    "magis-0",
    "magis-1",
    "magis-2",
    "cogito-0",
    "cogito-1",
]

[placement."secret:gpu-node-tagger.token"]
target = "/run/gpu-node-tagger.token"

[placement."secret:gput-node-tagger.token"]
target = "/run/gpu-node-tagger/token"
user = "root"
group = "root"
mode = "0400"

[placement."secret:k3s.token"]
target = "/run/k3s-secrets/k3s.token"

[placement."secret:nvidia-tagger.token"]
target = "/run/kubernetes-secrets/nvidia-tagger.token"
user = "root"
group = "root"
mode = "0400"
